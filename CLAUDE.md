# Claude Code 작업 규칙 — 대한민국 법령 통합검색기

이 저장소는 국가법령정보 공동활용 API를 이용해 법령·행정규칙·선택 지역의 자치법규를 함께 검색하는 Streamlit 앱이다. 이 문서는 Claude Code가 작업을 시작할 때 읽는 최소 규칙이다. 프로젝트의 사용자 의도, 이미 해결한 오류, 남은 작업은 [docs/CLAUDE_HANDOFF.md](docs/CLAUDE_HANDOFF.md)에 있다.

## 절대 변경하지 말 것

- AI가 유사어, 동의어, 관련 법령, 법적 결론을 임의로 만들어 검색하거나 표시하지 않는다.
- 검색어는 법률용어의 정확한 문구를 우선한다. 정확 문구 → 띄어쓰기만 다른 문구 → 모든 단어가 같은 문서에 있는 보조 결과 순서를 지킨다.
- 본문에 검색어의 정확 일치가 없으면 그 문서를 본문 결과로 표시하지 않는다. 방화구획 검색에서 방화만 있는 문서를 보여 준 과거 오류가 핵심 회귀 사례다.
- 본문 일치가 없을 때 문서의 일반 메타데이터나 임의 조문을 일치 본문처럼 표시하지 않는다.
- API 키, 키 파일의 실제 경로, 인증 URL의 OC 값은 소스·Git·SQLite·로그·오류 메시지·테스트 출력·화면에 넣지 않는다.
- 실제 config.local.toml, .streamlit/secrets.toml, SQLite 캐시, 가상환경, 로그는 커밋하지 않는다.

## 현재 제품 경계

- 현재 배포된 제품은 아니다. run.bat가 로컬 PC의 http://127.0.0.1:8501에서 Streamlit을 실행한다.
- localhost:58152 같은 주소는 대화 중 사용한 UI 목업/미리보기 서버일 수 있으며, 실제 검색 앱 서버가 아니다.
- 최종 확인은 항상 국가법령정보센터 공개 원문 및 소관기관 기준이다. 이 앱은 법률 자문·해석·결론을 제공하지 않는다.
- AI 요약은 의도적으로 제외되어 있다. 나중에 추가하더라도 검색·원문·정확 일치 문맥과 분리된 선택 기능이어야 한다.

## 개발 규칙

1. 먼저 [docs/CLAUDE_HANDOFF.md](docs/CLAUDE_HANDOFF.md)와 해당 테스트를 읽는다.
2. 기능 또는 버그를 바꾸기 전에 실패하는 pytest 회귀 테스트를 만든다.
3. 최소 코드로 통과시키고 관련 단위 테스트를 실행한다.
4. 끝에는 python -m pytest -v를 실행한다. 로컬 환경에 pytest가 없으면 테스트 의존성을 설치한 별도 환경을 사용하며, 인증값을 출력하지 않는다.
5. 변경 범위를 작게 유지한다. API 요청 형식·지역 코드·정렬 규칙·캐시 TTL은 사용자 요구나 회귀 테스트 없이 바꾸지 않는다.
6. Git 커밋 전 git diff --check, git status --short를 확인한다. 원격 push, 배포, 비공개 앱 초대, 저장소 공개 설정 변경은 사용자의 명시적 승인 없이는 하지 않는다.

## 핵심 파일

- src/lawsearch/app.py: Streamlit 화면과 세션 상태
- src/lawsearch/service.py: 검색, 정확 본문 검증, 24시간 SQLite 캐시 사용
- src/lawsearch/detail.py: 조·항·호·목 단위의 정확 문맥 추출
- src/lawsearch/query.py, regions.py: @평택 등의 지역 문법과 공식 코드 해석
- src/lawsearch/normalize.py: API 응답 표준화와 인증 없는 공개 원문 링크 변환
- tests/: 기능별 회귀 테스트
